terraform {
  required_version = ">= 1.6.0"

  required_providers {
    # Replace with the provider(s) this module needs. Use permissive (>=)
    # constraints in reusable modules; pin in the root config instead.
    # aws = {
    #   source  = "hashicorp/aws"
    #   version = ">= 5.40"
    # }
  }
}
