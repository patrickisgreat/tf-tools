# Resources and locals for this module. No `provider` blocks — the root config
# configures providers and passes them in.

locals {
  # Derive computed values here (merged tags, name prefixes, etc.).
}

# resource "<provider>_<type>" "this" {
#   name = var.name
#   tags = var.tags
# }
